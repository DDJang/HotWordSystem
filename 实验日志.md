
## **《HotWordSystem - 实验日志》**

**作者**: `邓锦坚`


### 一、 立项与技术选型

本次大作业要求实现一个“热词统计与分析系统”，核心是基于滑动窗口的流式数据处理。在仔细阅读要求后，我确定了项目的核心技术栈和初步开发路线。

*   **开发语言**: 选择 **C++17**。主要原因是课程要求，并且C++在性能上拥有无与伦比的优势，非常适合开发此类高性能后台服务。C++17标准提供了如 `std::filesystem`、结构化绑定等便捷的新特性，可以提升开发效率。
*   **核心依赖库**:
    *   **中文分词**: 作业建议直接使用现有工具。我调研后选择了 `cppjieba`，它是一个纯C++实现、性能优异且被广泛使用的分词库，能满足分词和词性标注的需求。
    *   **Web服务**: 为了快速实现一个可交互的Web界面，我选择了 `httplib.h`。它是一个仅头文件的轻量级HTTP库，无需复杂的编译和链接，能让我专注于API逻辑本身。
    *   **JSON处理**: 前后端通信必然涉及JSON。`nlohmann/json` 是现代C++中最流行、API最友好的JSON库，是该场景下的不二之选。

*   **初期目标**: 优先保证核心链路的正确性，即 **文本预处理 -> 滑动窗口统计 -> Top-K查询**。

### 二、 核心数据结构设计与迭代

这是项目中最具挑战性也最关键的部分。我的设计经历了几个阶段的迭代。

**1. 初版设计：队列 + 哈希表**

我最初的想法是严格按照滑动窗口的定义来实现：
*   使用一个 `std::deque` 存储窗口内的数据项（`{timestamp, words[]}`）。
*   使用一个 `std::unordered_map<string, int>` 作为实时词频计数器。

当新数据到来时，`push_back` 到队列尾部并更新哈希表；同时检查队头元素，如果时间戳过期则 `pop_front` 并相应地减少哈希表中的词频。这个设计满足了O(1)（均摊）的更新复杂度和高效的词频查询，可以满足基本要求。

**2. 发现问题：无法支持“动态调整窗口大小”**

很快我注意到了作业可选要求中的一个高级功能：“动态滑动窗口大小”。如果用户想把窗口从10秒扩大到10分钟，我最初的设计就无能为力了，因为那些早于10秒前的数据已经被彻底丢弃了。

**3. 核心架构确立：“物理存储 + 逻辑视图”双层模型**

为了解决上述问题，我构思并实现了项目中最核心的一个设计——**双层滑动窗口架构**。

*   **物理存储层 (`storage`)**: 我引入了一个 `std::map<long long, std::vector<string>>` 作为底层存储。它会保留比当前窗口大得多的数据（例如，保留1小时的数据）。选择 `std::map` 是因为它能按时间戳自动排序，为后续范围查询提供了便利。
*   **逻辑视图层 (`activeData`, `wordCounts`)**: 保留了原有的 `deque` 和 `unordered_map` 结构。但此时，它不再是唯一的数据源，而仅仅是`storage`中符合当前窗口时间范围的一个“**视图**”或“**缓存**”。

这个设计的**优点**是巨大的：
*   **实现了无损动态调窗**：当 `setWindowSize` 被调用时，我只需清空逻辑视图，然后遍历 `storage` 这个“数据母版”，将符合新窗口范围的数据重新加载到逻辑视图中即可。
*   **支持更复杂的分析**: 有了更长周期的原始数据，实现趋势分析等功能也变得水到渠成。

相应的，我也清楚地认识到其**设计取舍**：
*   **以空间换功能**: 这个架构会占用更多的内存。为此，我增加了 `maxRetentionSeconds` (最大保留时间) 和 `MAX_STORAGE_ENTRIES` (最大条目数) 两个配置，作为内存的硬顶限制，防止内存无限增长，并通过UI告警，实现了健壮性。

### 三、 系统模块化与整合

核心算法稳定后，我开始搭建整个应用的骨架。

*   **`SystemContext` 的引入**: 随着模块增多（文本处理器、窗口、持久化、监控器），模块间的依赖关系变得复杂。为了避免在构造函数中传递一大堆指针，我设计了 `SystemContext` 类。它作为所有核心模块的**中央协调器和持有者**，任何需要访问其他模块的组件（如 `APIServer`）只需持有 `SystemContext` 的引用即可。

*   **解决循环依赖**: 在实现中，我遇到了一个经典的C++问题：`SystemContext.h` 需要包含 `SlidingWindow.h` 来定义 `unique_ptr`，而 `SlidingWindow.h` 也想包含 `SystemContext.h` 来访问全局状态（如驱逐标志位），这导致了头文件循环依赖。解决方案是使用**前向声明 (`class SlidingWindow;`)**，在 `SystemContext.h` 中只声明 `SlidingWindow` 类，然后在 `SystemContext.cpp` 中再包含完整的头文件。这是一个非常重要的工程实践。

*   **`CommandExecutor` 的设计**: 为了统一处理来自CLI和Web的输入，我创建了 `CommandExecutor`。它使用一系列**正则表达式**来解析 `[ACTION]` 指令，将控制逻辑与输入源解耦。

### 四、 高级功能实现与Web界面开发

在坚实的后端架构之上，我开始逐一实现高级功能并开发前端界面。

*   **动态配置与持久化**: 我认为让用户能实时调整系统行为是提升可用性的关键。因此，我为词性过滤和敏感词过滤设计了 `/api/config` 接口。当用户在UI上修改配置时，后端不仅会更新内存中的 `unordered_set`，还会将敏感词列表**写回到 `sensitive_words.txt` 文件中**，实现了配置的持久化。

*   **前端开发**:
    *   **UI风格**: 我选择了现代化的 **液态玻璃** 风格，并使用动态渐变背景，希望提供一个美观且专业的界面。
    *   **可视化**: `ECharts` 是数据可视化的首选，我用它实现了实时更新的柱状图。
    *   **交互体验**: 我摒弃了原生的 `alert()`，自己实现了一套通知（Notification）和模态框（Modal）系统，用于提供更友好的用户反馈，例如操作确认、性能展示和错误告警。

*   **性能监控**: 我认为一个完整的系统需要能洞察自身的运行状态。因此，我创建了 `PerformanceMonitor` 模块，通过 `std::chrono` 记录耗时，通过平台API (`GetProcessMemoryInfo`) 获取内存，并通过 `/api/stats` 接口将QPS、内存占用等信息暴露给前端。

### 五、 测试与最终完善

项目的最后阶段是确保其稳定性和正确性。

*   **单元测试与集成测试**: 我编写了 `test_main.cpp` 测试程序。起初，我遇到了上文提到的ODR链接错误。通过将 `SystemContext` 的实现分离到独立的 `SystemContext.cpp` 文件中并进行链接，我解决了这个问题。测试用例不仅覆盖了 `GlobalUtils` 等独立模块（单元测试），还重点测试了 `CommandExecutor` 驱动整个系统状态变化的场景（集成测试）。

*   **性能优化思考**: 在 `TextProcessor::process` 函数中，我意识到了一个潜在的锁性能瓶颈。如果分词一个很长的句子，会长时间持有锁，阻塞其他请求。因此，我采用了“**读时复制**”的优化策略：先在锁内快速拷贝一份配置（如敏感词集合）到函数局部变量，然后立即释放锁，在无锁状态下执行耗时的分词操作。这极大地缩短了锁的持有时间，提升了并发性能。

### 六、 总结与反思

通过本次大作业，我不仅深入实践了滑动窗口这一核心数据结构，更完整地体验了从后端架构设计、API实现、前端开发到性能测试的全栈开发流程。

*   **最大的收获**: 莫过于设计并实现了“物理存储+逻辑视图”的双层窗口模型。它完美地平衡了功能（动态调窗）与资源（内存占用）之间的关系，是整个项目的技术核心和亮点。
*   **待改进之处**: 当前的全局锁机制虽然保证了绝对的数据一致性，但在极高的并发下可能成为瓶颈。未来可以探索更细粒度的锁，或者引入生产者-消费者模型来彻底解耦Web请求处理与后台核心计算，以追求更高的吞吐极限。

总而言之，HotWordSystem项目是一个功能完备、架构合理、交互现代的高性能分析系统。整个开发过程不仅锻炼了我的编码能力，更极大地提升了我在系统设计、性能优化和工程实践方面的综合能力。
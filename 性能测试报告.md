## **HotWordSystem 高性能实时热词分析系统 - 性能测试报告**

### 1. 测试环境与数据生成方式

#### 1.1 测试环境

| 组件         | 配置                                                                 | 备注                                           |
| :----------- | :------------------------------------------------------------------- | :--------------------------------------------- |
| **硬件**     | CPU: Intel Core(TM) Ultra 7 155H                            | 性能测试对 CPU 核心数和主频敏感           |
|              | 内存: 32 GB LPDDR5 7467                                             | 监控内存增长情况                               |
|              | 磁盘: FORESEE XP2000G001T                                                  | 减少持久化I/O操作对测试结果的干扰              |
| **软件**     | 操作系统: Windows 11 25H2                                    | 与项目开发环境保持一致                         |
|              | 编译器: GCC 15.1.0                             |                                                |
|              | 构建类型: **Release**                                                | **必须使用 Release 构建以启用所有编译器优化** |
| **测试目标** | HotWordSystem.exe |                             |

#### 1.2 数据生成与负载注入方式

本次测试采用两种互补的方式来评估系统性能：

1.  **内部基准测试 (Internal Benchmark)**
    *   **方式**: 使用系统内置的 `[ACTION] BENCHMARK N=...` 指令。
    *   **数据源**: 由 `GlobalUtils::generateRandomSentence()` 函数在**内存中**实时生成随机中文句子。
    *   **测试目的**: 此方式绕过了网络通信、HTTP解析和JSON序列化等外部开销，专注于测试系统**核心处理链路（文本处理 -> 滑动窗口计算 -> 持久化写入）的极限吞吐能力**，作为性能基线的最高参考值。

2.  **外部压力测试 (External Stress Test)**
    *   **方式**: 使用项目提供的 `scripts/client.py` 脚本。
    *   **数据源**: 从 `scripts/test_data.txt` 文件中读取预定义的文本行。
    *   **测试目的**: 模拟真实世界的使用场景，测试整个系统的**端到端综合性能**。其结果会受到网络延迟、`httplib` 的处理能力以及 `nlohmann/json` 的解析性能影响，更能反映系统在实际部署中的表现。

---

### 2. 核心性能指标测试

#### 2.1 测试一：极限吞吐能力基准测试

*   **测试条件**:
    *   启动 `HotWordSystem.exe` 服务。
    *   在Web UI或CLI中，执行 `[ACTION] BENCHMARK N=100000` 指令（注入10万条数据）。
    *   在指令执行期间，通过Web UI的性能弹窗或 `/api/stats` 接口，记录系统达到峰值时的性能数据。
    *   重复测试3次，取平均值。

*   **测试结果表格**:

| 指标 (Metric)          | 平均值 (Average Value) | 备注 (Notes)                                   |
| :--------------------- | :--------------------- | :--------------------------------------------- |
| **峰值吞吐量 (QPS)**   | `3698.80` lines/sec   | 反映核心处理链路的极限速度                     |
| **峰值CPU占用率**      | `32` %           | 观察CPU是否成为瓶颈                            |
| **任务完成时内存占用** | `142.38` MB          | 10万条数据 (`MAX_STORAGE_ENTRIES`) 的内存占用 |
| **总耗时**             | `27.47` s           | 完成10万条数据注入所需的总时间                 |
| **查询 Top-10**             | `35` us           | 查询 Top-10 的平均用时                 |



#### 2.2 测试二：大文本持续注入运行稳定性测试 (内存泄漏检测)

*   **测试条件**:
    *   启动 `HotWordSystem.exe` 服务。
    *   用脚本注入 `100'000` 条文本。
    *   执行对应次数后，记录系统的内存占用。

*   **测试结果表格**:

| 次数       | 内存占用 (MB) | 备注 (Notes)                                                 |
| :----------- | :------------ | :----------------------------------------------------------- |
| 0   | `14.36`    | 初始内存占用                                                 |
| 5  | `330.16`    | 弹出超过内存限制危险弹窗          |
| 7  | `332.39`    | 内存持续平稳，无明显线性增长                         |
| 9  | `333.45`    | 最终内存占用，与第 5 次时基本持平                    |

---

### 3. 分析与改进建议

#### 3.1 预期结果分析

1.  **CPU瓶颈分析**: 根据系统设计，性能瓶颈预计主要出现在CPU密集型任务上，包括：
    *   `TextProcessor` 中的 `cppjieba` 分词计算。
    *   `SlidingWindow` 中向 `std::map` 插入数据时的对数时间开销。
    *   高并发下，所有线程对 `SystemContext` 中 `global_mutex` 的**锁竞争**。

2.  **吞吐量与并发关系**:
    *   在并发数较低时（1-10），预计系统吞吐量会随并发数的增加而**线性增长**。
    *   当并发数进一步提高，由于全局锁 `global_mutex` 的存在，预计吞吐量增速将**显著放缓并达到一个平台期**。如果并发数过高，因线程切换和锁等待的开销，吞吐量甚至可能轻微下降。

3.  **内存稳定性**:
    *   在长时间稳定性测试中，内存占用应呈现**初期快速增长，然后趋于稳定**的健康状态。稳定值取决于 `maxRetentionSeconds` 和 `MAX_STORAGE_ENTRIES` 的设置。如果内存持续线性增长且不释放，则表明存在内存泄漏。

#### 3.2 架构改进建议

1.  **并发性能优化 (核心)**:
    *   **瓶颈**: 当前的全局锁 `global_mutex` 设计简洁，但在高并发下是明显的性能天花板。
    *   **建议**: 引入更细粒度的锁，或向**无锁/队列化架构**演进。
        *   **方案A (细粒度锁)**: 为 `SlidingWindow`, `PersistenceManager`, `TextProcessor` 的配置等模块提供独立的互斥锁。
        *   **方案B (生产者-消费者模型)**: `APIServer` 和 `CommandExecutor` 作为生产者，将解析后的数据（如 `struct ParsedData { long long ts; std::vector<std::string> words; }`）放入一个线程安全的并发队列中。`SlidingWindow` 和 `PersistenceManager` 在独立的后台工作线程中作为消费者，从队列中取出数据进行处理。这将彻底解耦Web服务与核心计算，最大化并发效率。

2.  **I/O 性能优化**:
    *   **瓶颈**: `PersistenceManager::logData` 当前是同步写入磁盘文件。在高QPS下，磁盘I/O可能成为瓶颈。
    *   **建议**: 将日志写入操作异步化。`logData` 方法可以将日志内容写入一个内存缓冲区（`std::vector<std::string>`），由一个专门的I/O线程负责定期、批量地将缓冲区内容刷入磁盘。

3.  **API 接口设计**:
    *   **现状**: 当前的 `/api/command` 接口通过一个统一的 `cmd` 字段来传递指令，类似一个RPC通道。
    *   **建议**: 对于面向外部的系统，可考虑设计更符合业界标准的 **RESTful API**。例如：
        *   `POST /api/data`: 提交新的数据行。
        *   `GET /api/stats`: 获取性能统计。
        *   `PUT /api/config/window`: 更新窗口大小。
        *   `POST /api/actions/reset`: 执行重置操作。
        *   这会使API的语义更清晰，更易于被其他服务集成和理解。

4.  **延迟监控**:
    *   **现状**: 系统目前缺乏对端到端延迟的直接监控。
    *   **建议**: 在 `APIServer` 的路由处理逻辑中，记录请求到达时间和响应发送时间，计算单次请求的处理耗时，并将其作为一个新的指标加入到 `/api/stats` 中，为性能优化提供更精确的数据。
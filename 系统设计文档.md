## **实时热词监控与控制系统 - 系统设计文档**

### 1. 背景、假设与外部依赖

#### 1.1 项目背景
本项目旨在构建一个高性能的实时热词分析系统。该系统能够从持续的文本数据流中，实时地提取、统计和分析高频词汇（热词），并通过一个现代化的Web界面进行可视化展示和交互式控制。系统的核心应用场景包括：实时监控舆情热点、分析用户评论、跟踪社交媒体趋势等。

#### 1.2 基本假设
*   **输入数据格式**: 系统主要处理两种格式的文本输入：
    1.  带时间戳的日志格式，如 `[HH:MM:SS] <文本内容>`。
    2.  纯文本格式，系统将为其自动分配递增的时间戳。
*   **语言环境**: 系统核心处理对象为中文文本，分词、过滤等功能均围绕中文特性设计。
*   **实时性要求**: 系统要求“软实时”，即数据从输入到在前端可视化界面上反映出来，延迟应在秒级范围内，满足人类用户的感知需求。
*   **运行环境**: 系统设计为独立的服务器应用程序，通过命令行启动。它内置Web服务器，用户通过浏览器访问。`CMakeLists.txt` 和部分源码（如内存监控）显示其优先考虑了Windows环境，但核心逻辑具有跨平台潜力。

#### 1.3 外部依赖
系统构建于一系列成熟的开源第三方库之上：
*   **`cppjieba`**: 高性能的C++中文分词库，用于实现核心的文本分词与词性标注功能。
*   **`httplib.h`**: 一个仅头文件的C++ HTTP/HTTPS库，用于快速构建轻量级的Web服务器（`APIServer`）。
*   **`nlohmann/json`**: 现代C++的JSON库，用于在前后端之间序列化和反序列化数据。
*   **`utfcpp`**: 用于在`TextProcessor`中安全、正确地处理UTF-8编码的字符串，进行清洗和标准化。
*   **`ECharts.js`**: 前端使用的强大数据可视化库，用于绘制实时热词柱状图。

### 2. 模块/架构图与数据流

#### 2.1 系统架构
本系统采用模块化、分层的架构设计，实现了高内聚、低耦合。整体可分为以下几个层次：



*   **输入与展示层 (Input & Presentation Layer)**:
    *   **Web UI (`dashboard.html`, `script.js`, `style.css`)**: 用户交互的主要界面，负责数据可视化和发送控制指令。
    *   **命令行接口 (CLI - `main.cpp`)**: 提供备用的文本输入和命令执行方式。

*   **调度与控制层 (Control & Dispatch Layer)**:
    *   **`APIServer`**: 接收来自Web UI的HTTP请求，并将其分发到`CommandExecutor`。它负责API路由和数据格式转换（JSON）。
    *   **`CommandExecutor`**: 系统的“总指挥”，解析所有输入（无论来自Web还是CLI），识别是数据还是指令，并调用下层核心服务来完成相应操作。

*   **核心服务层 (Core Service Layer)**:
    *   **`TextProcessor`**: 文本预处理引擎，负责分词、词性过滤、停用词和敏感词过滤。
    *   **`SlidingWindow`**: **系统的核心计算引擎**，在内存中维护一个滑动窗口，提供实时热词统计、趋势分析等功能。
    *   **`PersistenceManager`**: 持久化模块，负责将处理后的数据写入日志文件，并提供历史数据查询与报告生成功能。
    *   **`PerformanceMonitor`**: 性能监控模块，跟踪系统的吞吐量、内存占用等关键指标。

*   **上下文与状态层 (Context & State Layer)**:
    *   **`SystemContext`**: 系统的“黏合剂”，通过依赖注入的方式持有所有核心服务模块的实例和全局状态（如原子标志位、全局互斥锁），为上层模块提供统一的访问入口，并协调线程安全。

#### 2.2 核心数据流

**1. 数据注入流程 (Data Ingestion Flow):**
1.  用户通过 **Web UI** 的文本框输入数据，或通过 **CLI** 输入一行日志。
2.  请求被发送到 **`APIServer`** (`/api/command`) 或由 `main` 函数直接捕获。
3.  两者都将原始字符串传递给 **`CommandExecutor::process`** 方法。
4.  `CommandExecutor` 加锁 `SystemContext` 的全局互斥锁，然后调用 **`TextProcessor`** 对文本进行分词和过滤，得到一个词列表。
5.  `CommandExecutor` 将（时间戳，词列表）传递给 **`SlidingWindow::addData`**，更新内存中的实时统计。
6.  同时，数据也被传递给 **`PersistenceManager::logData`**，追加写入到 `history.log` 文件。
7.  最后，调用 **`PerformanceMonitor::record`** 更新性能计数器。
8.  `CommandExecutor` 释放锁，并返回处理结果。

**2. 实时图表查询流程 (Real-time Chart Query Flow):**
1.  **Web UI** (`script.js`) 中的定时器每秒向 **`APIServer`** 的 `/api/data` 接口发起一个GET请求。
2.  `APIServer` 接收请求，加锁 `SystemContext`。
3.  它调用 **`SlidingWindow::getTopK`** 方法，从内存中高效获取当前窗口内的Top-K热词。
4.  `APIServer` 将查询结果打包成JSON格式返回给前端。
5.  `APIServer` 释放锁。
6.  **Web UI** 收到JSON数据后，调用ECharts API，更新柱状图的展示。

### 3. 核心数据结构设计、复杂度分析、设计取舍

系统的核心在于`SlidingWindow`模块的设计，它采用了**物理存储与逻辑视图分离**的策略。

#### 3.1 核心数据结构
*   **物理存储 (`storage`)**: `std::map<long long, std::vector<std::string>>`
    *   **设计**: 使用 `std::map`，键为时间戳，值为该时间戳对应的词列表。
    *   **目的**: 保存比当前窗口大得多的历史数据（由`maxRetentionSeconds`控制，如1小时）。
    *   **优势**: `map`按键（时间戳）自动排序，这对于需要遍历一个时间范围的操作（如动态调整窗口大小、趋势分析）至关重要。

*   **逻辑视图 (`activeData`)**: `std::deque<std::pair<long long, std::vector<std::string>>>`
    *   **设计**: 使用双端队列 `std::deque` 模拟一个FIFO（先进先出）的队列。
    *   **目的**: 精确地表示当前滑动窗口内的数据。
    *   **优势**: 在队尾添加新数据 (`push_back`) 和在队头移除过期数据 (`pop_front`) 的平均时间复杂度都是O(1)，完美匹配滑动窗口的特性。

*   **实时词频统计 (`wordCounts`)**: `std::unordered_map<std::string, int>`
    *   **设计**: 使用哈希表 `std::unordered_map` 存储当前逻辑视图 (`activeData`) 中每个词的精确词频。
    *   **目的**: 为Top-K查询提供极速的词频访问。
    *   **优势**: 增、删、改、查的平均时间复杂度均为O(1)。

#### 3.2 复杂度分析
*   **数据添加 (`addData`)**: `O(L + log(Ns))`
    *   `L`是单次输入数据的词数。更新`wordCounts`和`activeData`的时间与`L`成正比。
    *   向`storage`(map)中插入数据的时间复杂度为`O(log(Ns))`，其中`Ns`是`storage`中的条目总数。
    *   数据淘汰操作是均摊O(1)的。

*   **Top-K 查询 (`getTopK(k)`)**: `O(Nw * log(k))`
    *   `Nw`是`wordCounts`中独立词汇的数量。
    *   算法遍历`wordCounts`，并维护一个大小为`k`的最小堆。这是计算Top-K的标准高效算法。

*   **窗口大小调整 (`setWindowSize`)**: `O(Ns')`
    *   这是系统中最耗时的操作之一。它需要清空逻辑视图，并遍历`storage`中新窗口范围内的所有数据(`Ns'`)来重建逻辑视图。

#### 3.3 设计取舍
*   **内存 vs. 动态功能 (核心取舍)**:
    *   **取舍**: 系统没有只保留`activeData`（最节省内存的方案），而是额外维护了一个更大的`storage`。
    *   **代价**: 占用了更多的内存。
    *   **收益**: 获得了**动态调整窗口大小**的关键能力。用户可以将窗口从10秒扩大到10分钟，系统无需重读原始文件，而是直接从`storage`中快速重建视图，提供了极佳的用户体验。

*   **`map` vs. `unordered_map` for `storage`**:
    *   **取舍**: 为`storage`选择了有序的`map`而非更快的`unordered_map`。
    *   **代价**: 插入性能略低 (`O(log N)` vs. `O(1)`)。
    *   **收益**: `map`的有序性是实现`setWindowSize`和趋势分析的基础。如果用`unordered_map`，这两个功能将难以实现。

*   **全局锁 vs. 细粒度锁**:
    *   **取舍**: 系统在`SystemContext`中采用了一个全局互斥锁`global_mutex`来保护所有核心操作。
    *   **代价**: 在极高的并发写入和读取下，可能会成为性能瓶颈。
    *   **收益**: 极大地简化了并发控制的逻辑，有效避免了复杂的死锁问题，保证了数据在各个模块间的绝对一致性。对于本系统的目标场景，这是一个合理且稳健的选择。

### 4. 滑动窗口定义与实时性保证

#### 4.1 滑动窗口的定义
本系统的滑动窗口是一个**基于时间的、右边界动态推进**的窗口。
*   **窗口大小 (`windowSizeSeconds`)**: 窗口所覆盖的时间跨度，例如600秒。
*   **右边界 (`currentTimeCursor`)**: 由系统处理过的最新数据的时间戳决定。
*   **左边界**: `currentTimeCursor - windowSizeSeconds`。
*   **滑动机制**: 当一个时间戳为`T`的新数据点被`addData`处理时，`currentTimeCursor`会更新为`max(currentTimeCursor, T)`。随后，系统会从`activeData`的队头开始检查，将所有时间戳小于或等于新左边界的数据点移除，并同步更新`wordCounts`中的词频。

#### 4.2 实时性保证
系统通过以下几点来保证其“软实时”特性：
1.  **高效的数据结构与算法**: 如前所述，数据添加和查询操作的复杂度都非常低，保证了后端处理的低延迟。
2.  **异步Web服务**: `APIServer`运行在独立的线程中，不会阻塞数据处理或CLI操作。
3.  **前端轮询机制**: Web界面通过每秒一次的轮询（Polling）从后端获取最新数据，这是用户感知到的“实时”刷新率。
4.  **并发控制**: 虽然全局锁可能引入排队，但由于核心操作非常快，锁的持有时间极短，因此在常规负载下对实时性的影响微乎其微。

### 5. 性能优化与资源评估方法

#### 5.1 已实施的性能优化
*   **最小化锁持有时间**: 在`TextProcessor`的`process`方法中，采用了“读时复制”(Copy-on-Read)的策略。它先在锁内快速复制配置（如敏感词列表）到局部变量，然后立即释放锁，在无锁状态下执行耗时的分词操作。这极大地提升了并发性能。
*   **后台任务线程化**: 压力测试 (`runBenchmark`) 在独立的线程中运行，避免了大量数据注入时对主线程和API服务的阻塞。
*   **高效算法选择**: Top-K查询使用最小堆，避免了对所有词进行全排序，显著降低了计算开销。
*   **数据结构优化**: 为不同场景选择最优的数据结构，如用`unordered_map`实现O(1)词频查询，用`deque`实现O(1)窗口滑动。

#### 5.2 资源评估与保护方法
系统内置了完善的资源监控和自我保护机制。
*   **性能监控**:
    *   `PerformanceMonitor`模块实时跟踪运行时间、处理总行数/词数，并计算吞吐量（QPS）。
    *   这些指标通过`/api/stats`接口暴露，并在Web UI的弹窗中可视化，为性能评估提供了直接的数据支持。
*   **内存监控**:
    *   `PerformanceMonitor`通过平台特定API (`GetProcessMemoryInfo`) 获取进程的物理内存占用。
    *   该指标同样通过`/api/stats`接口提供。
*   **内存保护机制**:
    *   **容量上限 (`MAX_STORAGE_ENTRIES`)**: `SlidingWindow`设置了一个物理存储的条目上限（如50万条）。一旦超出，系统会**强制从最老的数据开始删除**，防止内存无限增长导致程序崩溃。
    *   **时间上限 (`maxRetentionSeconds`)**: `SlidingWindow`会定期清理`storage`中超出最大保留时间的数据，常规地回收内存。
    *   **前端告警**: 当上述任一淘汰机制被触发时，后端会设置标志位，并通过`/api/data`接口通知前端。前端会弹出明确的警告通知，告知用户发生了数据清理。